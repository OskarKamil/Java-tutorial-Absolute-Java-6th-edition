package chapter18;

public class Sub3 {
	/*
	 * If you want to draw rectangles, ovals and lines on screen you can do that.*
	 * You need to redefine paint(Graphics g) class, call super.paint(g), and then
	 * all the methods like drawOval etc you need to invoke with g as calling
	 * object. For example: g.drawOval(X_FACE, Y_FACE, FACE_DIAMETER,
	 * FACE_DIAMETER);
	 */
}
